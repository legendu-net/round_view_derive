You have my permissions to

- read any file on the machine if it's readable to the current user
- make changes to files under this project only
- create and/or remove temporary files under this project only
- run the command `cargo` to build and test a Rust project 
- run shell command `uv`, `fish -n` and `fish_indent` to lint or test Python and fish scripts
- run the shell commands (e.g., `ls`, `cat`, etc) which does NOT change any files

If an action you need to take falls into one of the above categories,
directly proceed without asking for my approvals.
However,
please do NOT commit or stage changes so that I can review the diff locally.
